# `infino` is published on the public Gemfury proxy for now (not yet on public
# npm). The proxy serves infino + its platform packages and falls through to
# public npm for everything else. Remove this line once infino is on public npm.
registry=https://npm-proxy.fury.io/infino/
